import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export default function AdminServicesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Services</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Service management interface will go here.</p>
      </CardContent>
    </Card>
  );
}
