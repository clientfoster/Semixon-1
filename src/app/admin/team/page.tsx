import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export default function AdminTeamPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Team</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Team management interface will go here.</p>
      </CardContent>
    </Card>
  );
}
